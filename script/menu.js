const toggleSettingsOverlay = () => {
    new Promise((resolve) => {
        console.log('settings overlay toggled')
    });
}

const togglePause = () => {
    new Promise((resolve) => {
        console.log('game paused')
    }
    );
}

const saveGame = () => {
    new Promise((resolve) => {
        console.log('game saved')
    }
    );
}